import { PowerRange, CommodityQuantity } from "s2-typescript";

function init(){
    const powerRange = new PowerRange({ start_of_range: 0, end_of_range: 100, commodity_quantity: 'ELECTRIC.POWER.L1'});
    
}